import React from "react";
import RoleOffWorkspace from "@/pages/resource_management/roleoff/RoleOffWorkspace";

const RMRoleOffPage = () => {
  return React.createElement(RoleOffWorkspace, { mode: "rm" });
};

export default RMRoleOffPage;

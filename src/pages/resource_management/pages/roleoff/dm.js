import React from "react";
import RoleOffWorkspace from "@/components/roleoff/RoleOffWorkspace";

const DMRoleOffPage = () => {
  return React.createElement(RoleOffWorkspace, { mode: "dm" });
};

export default DMRoleOffPage;

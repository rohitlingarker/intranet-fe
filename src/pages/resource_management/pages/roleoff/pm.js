import React from "react";
import RoleOffWorkspace from "@/pages/resource_management/roleoff/RoleOffWorkspace";

const PMRoleOffPage = () => {
  return React.createElement(RoleOffWorkspace, { mode: "pm" });
};

export default PMRoleOffPage;
